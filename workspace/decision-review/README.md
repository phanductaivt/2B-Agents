# Decision Review UI

This folder is deprecated and compatibility-only.

Role note:
- Confirmation write decisions are now handled in `workspace/ops-console/`.
- This folder remains only as a legacy redirect surface.
- Do not build new features here.

Use:
```bash
python3 app.py --decision-workspace
```

Then open:
`http://127.0.0.1:8787`

Preferred:
```bash
python3 app.py --ops-console
```
