# Jira Connector

This folder holds a simple Jira-style connector placeholder.

Current purpose:
- build a basic story payload
- show where real Jira integration would live later

It is safe to treat this as a placeholder only. The current project writes final output as Markdown files, not Jira payload files.
