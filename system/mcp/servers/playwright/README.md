# Playwright MCP

Playwright MCP provides browser automation for local/dev UI inspection, smoke
testing, screenshots, console logs, and visible-state evidence.

Use it when the task requires actual browser behavior. Avoid production
mutation flows unless the user approves the exact action.
