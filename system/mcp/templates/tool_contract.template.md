# Tool Contract: `<MCP_SERVER_NAME>`

Define only the tools or behavior classes approved for this workspace.

## `<tool.name>`

- Purpose:
- Input:
  ```json
  {}
  ```
- Output:
  ```json
  {}
  ```
- Allowed scope:
- Risk level:
- Failure cases:
- Example request:
- Example response:

## Contract Rules

- Validate scope before execution.
- Support dry-run for write or sync operations.
- Log metadata only.
- Return non-sensitive errors.
- Block denied actions or request human approval.
