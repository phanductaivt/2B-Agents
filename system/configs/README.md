# Configs

This folder stores simple system-level configuration files for the framework.

Current config areas:
- model selection
- routing rules
- playbook selection
- permissions
- template mapping

Project-specific config is now stored inside each project:

`projects/<project-name>/project-config.yaml`

These system config files are written to be easy for BA/business users to read with light technical support.
