# Confluence Connector

This folder holds a simple Confluence-style connector placeholder.

Current purpose:
- build a basic page payload
- show where PRD publishing logic can live later

Use it as a placeholder only. The current project writes final output as Markdown files, not Confluence pages.
